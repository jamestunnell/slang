# String Values

Strings are immutable.

String interpolation occurs when an expression is enclosed using `#{}`, like `var str "my name is #{name}"`. The expression value must be string or have a `toString`  method.
